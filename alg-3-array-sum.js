const array = [1, 2, 3]

// const total = array.reduce( (preVal, currVal) => preVal + currVal)


// console.log(total);

function sumUp(numbers) {
    let sum = 0; // 1

    for (let i = 0; i < array.length; i++) {
        sum += numbers[i];
    }
    return sum; // 1
}

//Linear Time Complexity  => O(n)

console.log(sumUp(arra)); //8


const array = [1, 2, 3]

// const total = array.reduce( (preVal, currVal) => preVal + currVal)


// console.log(total);

function sumUp(numbers) {
    let sum = 0; // 1

    for (let i = 0; i < array.length; i++) {
        sum += numbers[i];
    }
    return sum; // 1
}

//Linear Time Complexity  => O(n)

console.log(sumUp(arra)); //8




